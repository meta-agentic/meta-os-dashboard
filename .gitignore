node_modules/
dist/
instance.config.json
github.config.json
pages.config.json
.pages/
public/snapshots/
.cache/
.DS_Store

# per-user dashboard boards (server persistence)
.data/

# Agent runtime state — absolute local paths, churns every session (never portable).
.claude-flow/
.swarm/
ruvector.db
.claude/proven-config.json
.claude/.proven-config-version

# filled-in launchd manifests (absolute paths, machine-local) — templates are
# dashboard.plist.example and heartbeat.plist.example
*.metaos.dashboard.plist
*.metaos.heartbeat.plist
